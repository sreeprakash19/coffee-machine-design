import { PositionChannel } from '../../../channel';
import { Config } from '../../../config';
import { Mark, MarkDef } from '../../../mark';
import { VgValueRef } from '../../../vega.schema';
import { ScaleComponent } from '../../scale/component';
import { UnitModel } from '../../unit';
/**
 * Return encode for point (non-band) position channels.
 */
export declare function pointPosition(channel: 'x' | 'y', model: UnitModel, { defaultPos, vgChannel }: {
    defaultPos: 'mid' | 'zeroOrMin' | 'zeroOrMax';
    vgChannel?: 'x' | 'y' | 'xc' | 'yc';
}): {
    [x: string]: VgValueRef | VgValueRef[];
};
export declare function pointPositionDefaultRef({ model, markDef, config, defaultPos, channel, scaleName, scale, mark, checkBarAreaWithoutZero: checkBarAreaWithZero }: {
    model: UnitModel;
    markDef: MarkDef;
    config: Config;
    defaultPos: 'mid' | 'zeroOrMin' | 'zeroOrMax';
    channel: PositionChannel;
    scaleName: string;
    scale: ScaleComponent;
    mark: Mark;
    checkBarAreaWithoutZero: boolean;
}): () => {
    field: {
        group: string;
    };
    value?: undefined;
} | {
    value: import("../../../channeldef").ValueOrGradientOrText;
    field?: undefined;
} | {
    scale: string;
    value: number;
} | {
    mult: number;
    signal: string;
    scale?: undefined;
    value?: undefined;
};
//# sourceMappingURL=position-point.d.ts.map